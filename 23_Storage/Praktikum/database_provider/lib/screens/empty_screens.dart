import 'package:flutter/material.dart';

class EmptyScreens extends StatelessWidget {
  const EmptyScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
